<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:WPR">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles/Base.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type local:Badge}" BasedOn="{StaticResource BaseControl}">
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Background" Value="{DynamicResource AccentColorBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource AccentContrastColorBrush}"/>
        <Setter Property="Padding" Value="5,2"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="HorizontalContentAlignment" Value="Right"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="local:Extra.BorderRadius" Value="12"/>
        <Setter Property="local:Extra.Shadow" Value="{StaticResource ButtonShadow}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Badge}">
                    <Grid>
                        <Border x:Name="BadgeBorder" Background="{TemplateBinding Background}"
                                Margin="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BadgeMargin}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(local:Extra.BorderRadius)}"
                                MinHeight="10" MinWidth="{Binding  Path =ActualHeight, RelativeSource={RelativeSource Self}}"
                                Effect="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(local:Extra.Shadow)}">
                            <ContentPresenter Content="{TemplateBinding Content}"
                                              Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="BadgeVisible" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"></Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>