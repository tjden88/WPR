<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:wpr="clr-namespace:WPR">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles/Base.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type wpr:Card}" BasedOn="{StaticResource BaseControl}">

        <Setter Property="wpr:Extra.BorderRadius" Value="4"/>
        <Setter Property="Margin" Value="10"/>
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundColorBrush}" />
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="wpr:Extra.Shadow" Value="{StaticResource CardShadow}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type wpr:Card}">
                    <Grid Background="Transparent">
                        <Border x:Name="ShadowBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding Path=(wpr:Extra.BorderRadius), RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                Effect="{Binding Path=(wpr:Extra.Shadow), RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                        <wpr:ClippingBorder ClipToBounds="True"
                                                 BorderBrush="{TemplateBinding BorderBrush}"
                                                 BorderThickness="{TemplateBinding BorderThickness}"
                                                 CornerRadius="{Binding Path=(wpr:Extra.BorderRadius), RelativeSource={RelativeSource Mode=TemplatedParent}}">
                            <ContentPresenter/>
                        </wpr:ClippingBorder>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPopupShadowStyle" Value="True">
                            <Setter Property="wpr:Extra.Shadow" Value="{DynamicResource PopupBigShadow}"/>
                            <Setter Property="Margin" Value="20"/>
                        </Trigger>
                        <Trigger Property="IsDialogShadowStyle" Value="True">
                            <Setter Property="wpr:Extra.Shadow" Value="{DynamicResource DialogShadow}"/>
                            <Setter Property="Margin" Value="20"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>