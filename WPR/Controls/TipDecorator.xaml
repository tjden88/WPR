<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:WPR">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Styles/Base.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--Текст в овальной рамке с тенью-->
    <Style x:Key="RoundedDecorator2" TargetType="{x:Type ContentControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">

                    <Grid>
                        <Border Background="{TemplateBinding Background}"
                                Effect="{StaticResource SmallShadow}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding Path=(local:Extra.BorderRadius), RelativeSource={RelativeSource Mode=TemplatedParent}}">
                        </Border>
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style TargetType="{x:Type local:TipDecorator}">
        <Setter Property="HorizontalContentAlignment" Value="Left"></Setter>
        <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        <Setter Property="local:Extra.Shadow" Value="{StaticResource SmallShadow}"></Setter>
        <Setter Property="local:Extra.BorderRadius" Value="13"/>
        <Setter Property="Padding" Value="15,6"></Setter>
        <Setter Property="Background" Value="{DynamicResource SecondaryBackgroundColorBrush}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TipDecorator}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Margin="0,-5000" Name="TipRoot" Grid.Column="0" Grid.Row="1">
                            <ContentControl x:Name="Tip"
                                            IsHitTestVisible="False"
                                            Margin="-5000,0,2,0"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            RenderTransformOrigin="1,1">
                                <Grid Margin="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=TipMargin}">
                                    <Border 
                                        Background="{TemplateBinding Background}"
                                        Effect="{Binding Path=(local:Extra.Shadow), RelativeSource={RelativeSource Mode=TemplatedParent}}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="{Binding Path=(local:Extra.BorderRadius), RelativeSource={RelativeSource Mode=TemplatedParent}}">

                                    </Border>
                                    <ContentPresenter Margin="{TemplateBinding Padding}" Content="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Tip}"
                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Grid>
                                <ContentControl.RenderTransform>
                                    <ScaleTransform ScaleX="0" x:Name="ScaleDecorator"/>
                                </ContentControl.RenderTransform>
                            </ContentControl>
                        </Border>
                        <ContentPresenter Grid.Row="1" Grid.Column="1" x:Name="ContentPresenter" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HorizontalContentAlignment" Value="Left">
                            <Setter Property="Margin" TargetName="Tip" Value="2,0,-5000,0"/>
                            <Setter Property="RenderTransformOrigin" TargetName="Tip" Value="0,0"/>
                            <Setter TargetName="TipRoot" Property="Grid.Column" Value="2"></Setter>
                        </Trigger>

                        <Trigger Property="HorizontalContentAlignment" Value="Center">
                            <Setter Property="Margin" TargetName="Tip" Value="-5000,0,-5000,0"/>
                            <Setter Property="RenderTransformOrigin" TargetName="Tip" Value="0.5,0.5"/>
                            <Setter TargetName="TipRoot" Property="Grid.Column" Value="1"></Setter>
                        </Trigger>

                        <Trigger Property="VerticalContentAlignment" Value="Top">
                            <Setter TargetName="TipRoot" Property="Margin" Value="0,0,0,-5000"></Setter>
                            <Setter TargetName="TipRoot" Property="Grid.Row" Value="2"></Setter>
                        </Trigger>

                        <Trigger Property="VerticalContentAlignment" Value="Bottom">
                            <Setter TargetName="TipRoot" Property="Margin" Value="0,-5000,0,0"></Setter>
                            <Setter TargetName="TipRoot" Property="Grid.Row" Value="0"></Setter>
                        </Trigger>

                        <Trigger Property="IsMouseOver" SourceName="ContentPresenter" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Duration="0:0:0.3"
                                                 To="1"
                                                 Storyboard.TargetName="ScaleDecorator"
                                                 EasingFunction="{StaticResource ElasticEaseOut}"
                                                 Storyboard.TargetProperty="ScaleX"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Duration="0:0:0.2"
                                                 To="0"
                                                 Storyboard.TargetName="ScaleDecorator"
                                                 Storyboard.TargetProperty="ScaleX"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>
</ResourceDictionary>